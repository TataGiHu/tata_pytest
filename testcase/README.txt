
REMDME: Testcase directory is used to store test cases. 
         You can customize your own test modules here.

   By default:

    - Name the test module(.Py) with test_[name];

    - Name the test class with test[name] (there can be no "__init__" method);

    - Name the test function with test[name].